async function main(): Promise<void> {
  console.log("Seed stub — implement in T-API-04");
}

main()
  .then(() => process.exit(0))
  .catch((error: unknown) => {
    console.error(error);
    process.exit(1);
  });
